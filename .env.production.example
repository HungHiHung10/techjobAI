# Frontend production env for Vercel.
# These values are public because Vite exposes VITE_* to the browser.
# Never put LLM provider keys here.

VITE_API_URL=https://YOUR_BACKEND_DOMAIN/api
VITE_WS_URL=wss://YOUR_BACKEND_DOMAIN/ws/chat
VITE_USE_MOCK=false
VITE_CHAT_TIMEOUT_MS=90000
